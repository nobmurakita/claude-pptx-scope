name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          LDFLAGS="-s -w -X github.com/nobmurakita/claude-pptx-scope/internal/cmd.Version=${GITHUB_REF_NAME}"
          TARGETS=(
            "darwin/amd64"
            "darwin/arm64"
            "linux/amd64"
            "linux/arm64"
            "windows/amd64"
          )
          mkdir -p skills/pptx-scope/scripts
          for TARGET in "${TARGETS[@]}"; do
            GOOS="${TARGET%/*}"
            GOARCH="${TARGET#*/}"
            OUTPUT="skills/pptx-scope/scripts/pptx-scope-${GOOS}-${GOARCH}"
            if [ "$GOOS" = "windows" ]; then
              OUTPUT="${OUTPUT}.exe"
            fi
            echo "Building ${OUTPUT}..."
            CGO_ENABLED=0 GOOS="$GOOS" GOARCH="$GOARCH" \
              go build -ldflags="$LDFLAGS" -o "$OUTPUT" ./cmd/pptx-scope
          done

      - name: Create zip
        run: |
          cd skills/pptx-scope
          zip -r "../../pptx-scope-${GITHUB_REF_NAME}.zip" \
            SKILL.md \
            scripts/pptx-scope \
            scripts/pptx-scope-darwin-amd64 \
            scripts/pptx-scope-darwin-arm64 \
            scripts/pptx-scope-linux-amd64 \
            scripts/pptx-scope-linux-arm64 \
            scripts/pptx-scope-windows-amd64.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: "pptx-scope-${{ github.ref_name }}.zip"
          generate_release_notes: true
